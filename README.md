# Lab 5 - Starter
Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!
- Yezhi Wu

---

### Answers
1). No, unit test cannot test how individual components interact on the feature level.  
2). Yes, you can easily write a test that passes some string that oversize to a validation function and asserts that it return `true` or `false`.  